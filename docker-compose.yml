version: '2'

services:
  web:
    build: .
    command: python server.py
    ports:
      - "5000:5000"
    volumes:
      - .:/application
    links:
      - postgres
    depends_on:
      - postgres
  postgres:
    image: "postgres:9.4"
    volumes:
      - data-postgres:/var/lib/postgresql/data

volumes:
  data-postgres:
